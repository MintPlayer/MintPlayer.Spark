# Plan — Controllers that obey Spark's rules, an honest group vocabulary, and opt-in auth routes

**PRD:** `docs/issue_298_300_301_302_303_PRD.md`
**Issues:** #298, #300, #301, #302, #303
**Branch:** `feat/issue-298-300-301-302-303-controllers-groups-routes` (from `master` @ `2bfd542`)
**Release:** `10.0.0-preview.60`, `@mintplayer/ng-spark-auth@23.0.0`

**Commit policy:** one commit per milestone, referencing the issue it serves. The full suite runs once, at M12. Intermediate milestones are verified by build and by reading.

**Sequencing note.** #298 lands before #301's `[SparkAuthorize]`, because the attribute must target the new vocabulary. #300 is independently shippable and goes first — it is the smallest, the most urgent (five live unprotected write endpoints, one minting credentials), and it needs no new public types.

---

## Milestones

| | Milestone | Issue | State |
|---|---|---|---|
| **S1** | Spike — is `MapControllers()` idempotent, and do Spark's conventions reach app-mapped controllers? | #300 | not started |
| **S2** | Spike — deny-by-default antiforgery against the five CodeCoverage endpoints, in a browser | #300 | not started |
| **S3** | Spike — `ISparkRowRule<T>` shares the scoped memo (hooks invoked once per `(type, action)`) | #301 | not started |
| **S4** | Spike — `ApplyAsync` over a `[FromIndex]` projection query | #301 | not started |
| **S5** | Spike — `[SparkAuthorize]` via `IAuthorizationRequirementData`, end to end | #301 | not started |
| **S6** | Spike — reserved group ids are unreachable through a custom `IGroupMembershipProvider` | #298 | not started |
| **S7** | Spike — `*ngTemplateOutlet` inside `@for` under OnPush + zoneless, with a server-driven list | #303 | not started |
| **M1** | Antiforgery: invert the default within `PathPrefixes` | #300 | not started |
| **M2** | `spark.AddControllers(…)` module | #300 | not started |
| **M3** | Row hooks onto `IPersistentObjectActions<T>` (breaking) | #301 | not started |
| **M4** | `ISparkRowRule<T>` — `ApplyAsync` + the three primitives | #301 | not started |
| **M5** | `[SparkAuthorize]` | #301 | not started |
| **M6** | Group vocabulary: `wellKnown` ids, `anonymous`/`authenticated`, `Everyone` removed | #298 | not started |
| **M7** | `canQuery`, denial-expansion rejection, resource validation | #298 | not started |
| **M8** | Startup security-posture summary + `--spark-verify-security` | #298 | not started |
| **M9** | `sparkAuthRoutes` feature functions; server default `Disabled` (breaking) | #302 | not started |
| **M10** | `providerTemplate` projection; navigation-level absence tests | #303 #302 | not started |
| **M11** | Demos + `security.json` migration | all | not started |
| **M12** | Docs, release notes, version bumps, full suite | all | not started |

---

## Spikes

### S1 — `MapControllers()` idempotency

**Question.** D5 ("wrap, don't forbid") rests entirely on this: does a second `MapControllers()` reuse the single `ControllerActionEndpointDataSource`, producing no duplicate endpoints and no `AmbiguousMatchException`? And does a convention Spark adds via *its* call apply to controllers the app mapped via *its own* call?

**Method.** A `TestServer` host with a trivial controller. Call `MapControllers()` twice, assert over `EndpointDataSource` that each action appears once. Then add a convention through Spark's call and assert the metadata is present on an endpoint materialized by the app's call.

**Negative result →** `spark.AddControllers` must not map at all, and the docs must tell the app to keep calling `MapControllers()` itself while Spark contributes only the convention. Do **not** attempt to forbid the call — a library cannot detect it.

### S2 — deny-by-default antiforgery in a real browser

**Question.** Does the Angular SPA actually send `X-XSRF-TOKEN` on the five CodeCoverage writes? And does the non-ambient exemption hold under a real `covt_` API token **and** a GitHub OIDC JWT?

**Method.** Run CodeCoverage against the new middleware with `RequireAntiforgery = true`, drive the five endpoints from the SPA, and drive `UploadsController` with both non-ambient schemes. `Coverage\ClientApp\src\spark-auth.setup.ts:34` suggests the interceptor is wired — **that must be tested, not assumed.**

**Negative result →** if the SPA does not send the token, the warning-only mode becomes the shipped default and the flip moves to the next major. If the exemption misses a scheme, `IsNonAmbientCredential` needs widening before this ships.

### S3 — memo sharing

**Question.** F7: the per-request memo holds hook invocations to one per `(type, action)` because Raven caps a session at 30 requests. Does `ISparkRowRule<T>` delegating into the scoped `RowSecurity` actually share it?

**Method.** Instrument the invocation counter. Issue one request that hits both a controller (via `ApplyAsync`) and a `/spark` query for the same type, and assert **one** invocation per `(type, action)`.

**Negative result →** the interface must take the memo explicitly, or `RowSecurity` must expose a scoped accessor. Shipping a second memo is not an option — it silently doubles I/O against the cap #239 exists to protect.

### S4 — `ApplyAsync` over a projection

**Question.** `ComposeRowFilterAsync` declines to compose when `elementType != entityType` (`RowSecurity.cs:232-245`) and falls back to `FilterAsync`'s base-document reload — which is session-bound and internal. Can `ApplyAsync` reproduce that path from a public API, or must it refuse projections?

**Method.** Call `ApplyAsync` with `session.Query<VRepository, Repositories_Overview>()`. CodeCoverage queries exactly this shape, so it is the **first** thing a real consumer hits.

**Negative result →** `ApplyAsync` must throw a clear "projection queries are not supported; query the entity type" rather than silently returning unfiltered rows. State the limit in the XML docs either way.

### S5 — `[SparkAuthorize]` end to end

**Question.** Does `IAuthorizationRequirementData` work in this host shape, with `AuthorizationMiddleware` where `UseSpark()` puts it (`SparkMiddleware.cs:200`) and a handler resolving a scoped `IAccessControl`?

**Method.** An MVC action with `[SparkAuthorize("Read", "Person")]`. Assert granted and denied paths, plus `[AllowAnonymous]` override. Confirm no pipeline reordering is needed.

**Negative result →** fall back to `SparkAuthorizationPolicyProvider : DefaultAuthorizationPolicyProvider` resolving `"Read/Person"` as a policy name, with base fallback for unknown names.

### S6 — reserved ids are unreachable by claim

**Question.** F11 says a claim naming `Authenticated` resolves the well-known id through `ResolveGroupIds`, bypassing the `IsAuthenticated` gate — falsifying a comment shipped in #306. Confirm before designing the exclusion.

**Method.** A stub `IGroupMembershipProvider` returning `["Authenticated"]` for an unauthenticated caller. Assert the group is granted **today** (RED), and not granted after M6.

**Negative result →** if it is already unreachable, this is a doc fix rather than a code fix and M6 shrinks.

### S7 — `*ngTemplateOutlet` under OnPush + zoneless

**Question.** Does writing `providers.set([...])` after first render re-stamp a projected template with the new context — with no `markForCheck`, no `ApplicationRef.tick()`, no zone?

**Method.** A host component passing `providerTemplate`, with `capabilities()` resolving late. Build on the timing shape already demonstrated at `spark-sign-in.component.spec.ts:114-126`.

**Negative result →** the whole projection design needs `ChangeDetectorRef` plumbing inside the component, which should be decided now rather than discovered in a consumer's app.

### Not spiked: the bundle-exclusion guarantee

`spark-auth-routes.ts:29-32` documents that an excluded page ships no chunk because the `import()` call site is unreachable. Not spiked because it is not testable at the vitest tier and not observable from Playwright — it is a **CI grep over the built bundle**, added in M12, not a spike.

### Not spiked: `In()` at scale

#301's unbounded-`In()` concern is real but is a *measurement*, not a design gate. Nothing in this PR's design depends on the answer, and a cap chosen without data would be arbitrary. Recorded as a follow-up in the PRD.

---

## M1 — antiforgery default inversion

**Files:** `SparkMiddleware.cs:215-237` (the switch from D-A); new `SparkControllersOptions` with `PathPrefixes` + `RequireAntiforgery` + a warning-only mode; `HasAmbientCredential` helper alongside the existing `IsNonAmbientCredential` (`:577`).

| Test | Pins |
|---|---|
| `A_cookie_authenticated_post_without_a_token_is_rejected` | A1 |
| `A_cookie_authenticated_post_with_a_token_succeeds` | A2 |
| `A_bearer_authenticated_post_is_unaffected` | A3 |
| `An_anonymous_post_is_unaffected` | A4 |
| `A_minimal_api_post_in_scope_is_covered` | A5 — **the discriminator against metadata stamping** |
| `DisableAntiforgery_still_exempts` | A6 |
| `A_path_outside_the_prefixes_is_unaffected` | scoping |
| `Warning_only_mode_logs_and_allows` | the migration path |

## M2 — `spark.AddControllers(…)`

**Files:** new `libs/spark/MintPlayer.Spark/Extensions/SparkBuilderControllersExtensions.cs`, following the 22-precedent idiom — `Action<TOptions>? configure = null`, `Spark:Controllers` config section bound first and code winning, `builder.Registry.AddEndpoints(e => e.MapControllers())`. No new package dependency (`MintPlayer.Spark.csproj` is `Microsoft.NET.Sdk.Web`). Optional `SparkFullOptions` property + generator emit line.

Must tolerate the app having already called `AddControllers()` (every demo does, *before* `AddSpark`) and must not discard its `.AddJsonOptions(...)`.

| Test | Pins |
|---|---|
| `MapControllers_called_twice_yields_one_endpoint_per_action` | A7, from S1 |
| `An_apps_earlier_AddControllers_configuration_survives` | hazard 10 |

## M3 — row hooks onto the interface (breaking)

**Files:** remove `[NoInterfaceMember]` from `GetRowFilterAsync`, `IsAllowedAsync`, `GetProtectedAttributesAsync` (`DefaultPersistentObjectActions.cs:55, 152, 224`); declare them on `IPersistentObjectActions<T>`; drop the corresponding reflection in `RowSecurity.cs:548-554` where it becomes dead.

Build the full solution and all four demos to size the break (S8 in the investigation).

## M4 — `ISparkRowRule<T>`

**Files:** new public interface in `MintPlayer.Spark.Abstractions`; scoped implementation delegating into `RowSecurity`; make `ICollectionGuard` public (12 lines, stateless — an app doing its own `LoadAsync<T>(id)` needs it for the same reason Spark does).

| Test | Pins |
|---|---|
| `ApplyAsync_applies_the_filter_and_the_per_row_hook` | A8 — type overriding **only** `IsAllowedAsync` |
| `ApplyAsync_pushes_the_filter_into_the_query_where_translatable` | RQL assertion |
| `GetFilterAsync_returns_null_for_an_unrestricted_type` | F6, documented semantics |
| `Hooks_are_invoked_once_per_type_and_action_per_request` | A9, from S3 |
| `ApplyAsync_over_a_projection_behaves_as_S4_determined` | A—, from S4 |

## M5 — `[SparkAuthorize]`

**Files:** attribute implementing `IAuthorizationRequirementData` + its `AuthorizationHandler`, in the Authorization package.

| Test | Pins |
|---|---|
| `A_granted_right_authorizes_the_action` | A10 |
| `A_denied_right_refuses_the_action` | A10 |
| `AllowAnonymous_overrides_it` | A10 |
| `The_group_form_resolves_through_the_membership_provider` | secondary form |

## M6 — group vocabulary

**Files:** `SecurityConfiguration` gains `WellKnown: Dictionary<string,string>`; `AccessControlService.cs:66-72` resolves by id and drops `GetDefaultValue()` matching; `ResolveGroupIds` excludes reserved ids; `anonymous` added when not authenticated; **`Everyone` removed** and its presence throws at load with migration text; **correct the false comment at `:51-54`.**

| Test | Pins |
|---|---|
| `A_claim_naming_a_reserved_group_does_not_grant_it` | A11 — **RED today** |
| `Reordering_translations_changes_no_decision` | A12 — **RED today** |
| `An_everyone_group_fails_startup_with_migration_text` | A13 |
| `An_anonymous_grant_stops_applying_once_signed_in` | A14 — the new semantic |
| `wellKnown_validation_rejects_unknown_key_missing_id_and_duplicate_id` | R11 |

## M7 — `canQuery`, denial rejection, resource validation

**Files:** `GetPermissions.cs:26-29` adds `canQuery`; reject a combined action used in a denial at load; validate `Right.Resource` against known actions and the three target namespaces — entity type names, the pseudo-target `LookupReferences`, and **plural collection names** for `Replicate/*` (`EtlDeploy.cs:80`).

| Test | Pins |
|---|---|
| `canQuery_is_reported_and_reflects_a_QueryRead_grant` | A15 |
| `A_combined_action_in_a_denial_is_rejected_at_load` | A16 |
| `A_Query_only_grant_lists_but_refuses_a_by_id_load` | D7 — proves no split is needed |
| `Validation_accepts_all_three_target_namespaces` | avoids crying wolf |

## M8 — posture summary + CI gate

**Files:** `ISecurityPostureReporter` in Abstractions (so `MintPlayer.Spark` need not reference the Authorization package); registered by Authorization; called from `SparkMiddleware.cs:305` right after `VerifySparkModelHash`, matching that precedent. Re-print on `SecurityConfigurationLoader`'s hot-reload invalidation. New `--spark-verify-security` mirroring `--spark-verify-model`.

Follow `ModelHashVerifier`'s stated principle — *"Warned on every startup, never once"* — and print the negative explicitly ("Anonymous callers can reach: nothing."), because silence is indistinguishable from the check not running.

**Throw** for malformed configuration; **log** for the posture itself. A public API app legitimately grants anonymous reads; refusing to start would be wrong.

| Test | Pins |
|---|---|
| `The_summary_lists_anonymously_reachable_types` | A17 |
| `The_summary_says_nothing_when_nothing_is_anonymous` | A17 |
| `AllowAnonymousAccess_and_AllowAll_print_warnings` | A17 |
| `The_summary_reprints_after_a_hot_reload` | staleness |
| `verify_security_exits_nonzero_on_a_changed_surface_without_a_database` | A18 |

## M9 — opt-in routes, server default `Disabled`

**Files:** `spark-auth-routes.ts` — `sparkAuthRoutes(...features)`; new `withLocalLogin()`, `withRegistration()`, `withExternalLogin(...providers)`, `githubProvider()`/`facebookProvider()` etc.; `SparkAuthenticationOptions.LocalCredentials` default → `Disabled`.

Keep the `import()` call sites inside each feature so the bundle-exclusion property survives (`spark-auth-routes.ts:29-32`).

| Test | Pins |
|---|---|
| `No_features_emits_no_routes` | A19 |
| `withLocalLogin_emits_the_password_family` | R18 |
| `withExternalLogin_emits_the_sign_in_landing_page` | R18 |
| `Navigating_to_an_un_opted_page_activates_no_spark_page` | A20 — navigation, not array shape |
| `The_server_default_is_Disabled` | R19 |
| `A_default_app_with_no_external_provider_fails_startup` | the loud consequence of R19 |

## M10 — provider template projection

**Files:** `providerTemplate` input + `SparkProviderButtonContext` on `SparkSignInComponent`; template `@if (providerTemplate(); as tpl)` with the current markup as `@else`.

New specs need a **host component** — the existing spec routes straight to the component (`:70-71`), and per F15 projection is unreachable that way. Carry `provideNoopAnimations()` forward or the alert branches throw.

| Test | Pins |
|---|---|
| `The_default_rendering_is_unchanged` | A21 — the 8 existing specs, unmodified |
| `A_projected_template_renders_per_provider` | A22 |
| `The_context_signIn_uses_the_servers_scheme` | A22 — the point of the closure |
| `The_empty_and_failure_branches_still_win` | branch precedence |

## M11 — demos and `security.json` migration

**Files:** `Demo/{Fleet,HR}/App_Data/security.json` — `wellKnown` block; each `QueryRead/Company` grant to `Everyone` becomes **two** grants (`anonymous` + `authenticated`), or an explicit named-group decision. ⚠️ **Fleet grants Company through `Everyone` and nowhere else**, so dropping the authenticated half regresses every signed-in Fleet user.

Also: HR has a **duplicated right id** (`b0000002-…-012` on both `ReadEditNew/Person` and `Replicate/Companies`) — harmless today since nothing reads `Right.Id`, but it should be fixed and covered by M7's validation.

Every demo's `Program.cs` gains an explicit `LocalCredentials` (they were relying on the old `Full` default) and every `app.routes.ts` gains explicit features. WebhooksDemo's shell uses `spark-auth-bar` and drops `loginWithGitHub()`.

Decide and record: WebhooksDemo currently passes `returnUrl: '/github-projects'`, and `SparkSignInComponent` has no equivalent. Either add a `returnUrl` input threaded into the context closure, or accept the default landing.

## M12 — docs, versions, sweep

**Files:** `docs/guide-authentication-schemes.md` (§5 rewritten for the new vocabulary), `docs/guide-queries-and-sorting.md`, a new controllers guide, `docs/release-notes-preview-60.md` leading with the four breaking changes; 21 `.csproj` → `preview.60`; `ng-spark-auth` → `23.0.0`.

Add the **bundle-exclusion CI check**: grep the built WebhooksDemo bundle for `SparkRegisterComponent` and fail if present.

---

## Verification

**Discriminating checks** — each must be demonstrated failing before its milestone:

1. Revert M1 → `A_minimal_api_post_in_scope_is_covered` must fail. This is the check that justifies inverting the default over stamping metadata; if a stamping implementation passes it, D3 is wrong.
2. Revert M4 → `ApplyAsync_applies_the_filter_and_the_per_row_hook` must fail for the `IsAllowedAsync`-only type. This is D4's justification.
3. Before M6 → `A_claim_naming_a_reserved_group_does_not_grant_it` and `Reordering_translations_changes_no_decision` must **both be RED on `2bfd542`**, confirming F10 and F11 are real and not misreadings.
4. Revert M9 → `No_features_emits_no_routes` must fail.

**Full suite** once at the end. Baseline on `2bfd542`: 1617 + 197 + 38 + 78 = **1930**, plus 79 vitest. Re-run any failure in isolation before calling it a regression; the known post-#277 E2E teardown hang is unrelated and unfixed.

**Not closed by this PR**, and carried forward again: the real GitHub OAuth round trip, and a browser run of `/sign-in`. Procedure is in `docs/issue_294_295_296_plan.md` § Verification. M10 makes the second one cheaper but does not replace it.
