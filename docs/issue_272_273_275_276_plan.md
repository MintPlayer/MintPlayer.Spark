# Plan — Issues #272 / #273 / #275 / #276

**PRD:** [issue_272_273_275_276_PRD.md](issue_272_273_275_276_PRD.md) ·
**Branch:** `fix/issues-272-273-275-276`

Test-first throughout: each fix milestone is preceded by (or opens with) failing tests pinning the
contract. The PR squashes, so red commits never reach `master`. Test suites run once, batched at
the end (M6), except the targeted spike/red-test runs each milestone explicitly calls for.

| | Milestone | Status |
|---|---|---|
| S1–S4 | Runtime spikes: Corax fault, repair, `No`-field behavior, nested-path null safety | **done 2026-08-19** (live 7.2.5; PRD F273.9–F273.11) |
| M1 | #272 — IndexRegistry restructure (red tests → fix) | pending |
| M2 | #273 Layer 1 — classifier + `FieldIndexing.No` + SPARK_INDEX_010 (red tests → fix) | pending |
| M2b | #273 Layer 2 index side — `[Breadcrumb]` property marker + one-hop sort companion (red tests → fix) | pending |
| M2c | #273 Layer 2 display side — JSON-authoritative templates, attribute deletion, embedded-token resolver arm | pending |
| M3 | #275 — Provenance-gated `query` preservation (red tests → fix) | pending |
| M4 | #276 — SparkQuery rename retarget + dead-source warning (red tests → fix) | pending |
| M5 | HR `Person` adopts `[GenerateIndex]` + `[Breadcrumb]`; demo re-sync; docs; release notes | pending |
| M6 | Full batched test sweep; lockstep version bump; PRD/plan status; PR | pending |

Ordering rationale: M1 before M2 because both touch `GenerateIndexDiagnostics.cs` (M1 removes
SPARK_INDEX_004, M2 adds SPARK_INDEX_010) and the registry's new semantics are what M2's runtime
tests route through. M3 before M4 only for reviewability — they edit disjoint regions of
`ModelSynchronizer.cs` (`:556-604` vs `:111-136`).

---

## S1–S4 — Spikes: DONE (2026-08-19, live RavenDB 7.2.5 at localhost:8080)

Statically answered during investigation: duplicate `Index()` → **throws** (`Dictionary.Add`,
PRD F273.4); `Color` persists as `"#rrggbb"` string, Fleet fine (F273.5).

Measured live (spike project in session scratchpad, `spike273/`; results in PRD F273.9–F273.11):

- **Engine-dependence**: the localhost server defaults to **Lucene**, where the verbatim complex
  map does NOT fault; forcing `SearchEngineType.Corax` reproduced the per-document
  `NotSupportedInCoraxException` (2/3 docs faulted; the null-`Address` doc indexed fine).
  ⇒ **All repro/repair tests in M2 must pin Corax explicitly** or they pass vacuously.
- **Repair pinned**: `FieldIndexing.No` + `StoreAllFields` → healthy index, complex fields fully
  populated through `ProjectInto`.
- **`No`-field behavior**: filter → 0 rows, sort → meaningless order, **no error either way** ⇒
  no `QueryExecutor` guard; doc note only.
- **Nested-path null safety**: `item.Address.City` (null `Address`) and
  `item.Address.Description.Label` (mid-path null) index as null, zero errors; sort (nulls first)
  and equality filter on the path scalar both work ⇒ the generator emits **no null guards**.
- **Collection fan-out**: `item.Jobs.Select(j => j.Title)` indexes cleanly (multi-valued; null →
  empty) but cannot sort — confirms collections stay Layer 1.
- **Redeploy**: byte-identical redeploy is a no-op (`CreatedTimestamp` unchanged).

The S1/S2 shapes become committed regression tests in M2
(`tests/MintPlayer.Spark.Tests/Services/ComplexFieldIndexingTests.cs`, `SparkTestDriver`-based,
modeled on `SearchPushdownTests.cs`, **Corax pinned per index**).

## M1 — #272: IndexRegistry restructure (PRD R272.1–R272.8)

**Files:** `libs/spark/MintPlayer.Spark/Services/IndexRegistry.cs`,
`libs/source_generators/MintPlayer.Spark.SourceGenerators/Diagnostics/GenerateIndexDiagnostics.cs`
(+ AnalyzerReleases tracking), `tests/MintPlayer.Spark.Tests/IndexRegistryTests.cs`,
`tests/MintPlayer.Spark.Tests/Model/ModelHashWriteTests.cs` (or sibling).

**M1a — Red tests** (fixture: add `TestCar_Search : AbstractIndexCreationTask<TestCar>` +
`VTestCarSearch` beside the existing types at `IndexRegistryTests.cs:6-16`):

1. `RegisterIndex_TwoIndexesForOneCollection_RetainsBoth` — plural lookup returns 2 (red: member
   doesn't exist); `GetAllRegistrations` returns 2 (green guard).
2. `GetRegistrationForCollectionType_WithDuplicates_ReturnsOrdinalMinDefault` — **both**
   registration orders assert the same winner (`TestCar_Overview` < `TestCar_Search` ordinal);
   at least one order is red under today's last-wins.
3. `RegisterProjection_AttachesToEachIndexIndependently` — per-name projections resolve;
   `IsProjectionType` true for the non-default projection (red today when it lost the slot).
4. Model-hash determinism: two registries, duplicate pair registered in opposite orders,
   `ModelSynchronizer` hash build on each → equal (red today).

Run only these; confirm red where predicted; commit.

**M1b — Fix:**

- `_byCollectionType` → `Dictionary<Type, List<IndexRegistration>>`, append via sorted insert
  (ordinal index name), all under `_lock`. `ContainsKey` for the name guard (drop unused
  `existing`). Registration-time `Console.WriteLine` warning when a list grows past 1, naming both
  indexes and the default.
- `IIndexRegistry`: add `IReadOnlyList<IndexRegistration> GetRegistrationsForCollectionType(Type)`;
  re-document the singular member as "the default registration" with the ordinal-min rule stated.
  (NSubstitute mocks in `ModelSynchronizerTests` pick up the new member automatically — no churn.)
- Singular lookup = element 0 of the list (default-first by construction); plural returns a copy.
- `IsProjectionType` scans all registrations (`_byIndexName.Values` — representation-independent).
- Remove the dead `EntityAlreadyHasHandWrittenIndex` descriptor (SPARK_INDEX_004, never reported;
  premise now false) + update analyzer release tracking. Check `docs/`/README for SPARK_INDEX_004
  mentions and update.
- Consumers (`QueryExecutor`, `DatabaseAccess`, `ModelSynchronizer`, `ModelShapeDiscovery`) keep
  their singular calls unchanged.

M1a tests green; also add a test pinning that a **stale model file for a non-default projection**
is cleaned up by `ModelSynchronizer.cs:219-230` (PRD Design note). Commit.

## M2 — #273 Layer 1: classifier + `FieldIndexing.No` + SPARK_INDEX_010 (PRD R273.1–R273.7)

**Files:** `libs/source_generators/MintPlayer.Spark.SourceGenerators/{Models/SparkModelSymbols.cs,
Generators/GenerateIndexGenerator.cs, Generators/GenerateIndexGenerator.Producer.cs,
Models/GeneratedIndexInfo.cs, Diagnostics/GenerateIndexDiagnostics.cs}`,
tests in `tests/MintPlayer.Spark.SourceGenerators.Tests/` and the runtime file below.

**M2-red — generator tests** (`GenerateIndexGeneratorTests.cs`, harness asserts on emitted text):

1. Nested object property (breadcrumb-less type) → expect
   `Index(nameof(VPerson.Address), global::Raven.Client.Documents.Indexes.FieldIndexing.No);`
   (red: nothing emitted today) + `NotContain("AddressSort")`.
2. `List<CarreerJob>` → `Index(...No)` (red). Counter-assert `List<string>` gets **no** `No` and
   stays `Search`-eligible (green guard).
3. User-defined struct property → `No` (red — the `IsValueType` trap).
4. `Color?` → no `Index(...No)`, no diagnostic (green guard — Fleet must not regress).
5. `Dictionary<string,string>` → `No` (red).
6. Complex + `[Search]` → SPARK_INDEX_005 still reported **and** no `FieldIndexing.Search` emitted
   for that field (red on the second half).
7. SPARK_INDEX_010 reported per breadcrumb-less complex property, Warning severity, message names
   both remedies (`[Breadcrumb]` on the type, `[IgnoreForIndex]`) (red: descriptor doesn't exist).
8. `ReferencedAssemblyEntityTests`: same entity via `CompileToMetadataReference` → same
   `Index(...No)` assertion (red).

**Runtime regression tests** (`tests/MintPlayer.Spark.Tests/Services/ComplexFieldIndexingTests.cs`,
`SparkTestDriver`, **`SearchEngineType.Corax` pinned per index** — the spike proved a
Lucene-defaulted server passes vacuously): the S1 fault repro (verbatim map, non-null complex →
index errors + missing entries; null-only → clean) and the S2 repair pin (`No` + stored →
healthy, `ProjectInto` and `QueryExecutor` round-trips populated).

**M2-fix:**

- `IsComplexForIndex` in `SparkModelSymbols` (beside `IsTranslatedString`): the PRD Layer-1
  allow-list, doc comment naming `SparkModelShape.IsComplexType`/`GetDataType` as the runtime twin
  and stating the deliberate divergences (user structs, dictionaries). Needs a symbol-level
  `GetCollectionElementType` twin (arrays via `IArrayTypeSymbol`, the six generic wrappers, any
  `IEnumerable<T>` except `char`).
- `Describe()`: complex → `FieldIndexing = "No"`, suppress `Search`/companion regardless of
  `[Search]`; collect into a new `ComplexProperties` list on `GeneratedIndexInfo`
  (`InvalidSearchProperties` pattern; `[AutoValueComparer]` — adding a property is safe).
  `DescribeReferenced()` inherits for free.
- `GenerateIndexDiagnostics`: add SPARK_INDEX_010 (Warning) + analyzer release tracking; report
  from `GetDiagnostics()` in the producer.
- Snapshot churn in `SourceGeneratorSnapshots.*.verified.txt`: accept as reviewable diff where the
  pinned fixture has complex properties; if it has none, extend the fixture so the new emission is
  pinned.
- No `QueryExecutor` guard for sort/filter on a `No` field (measured benign degradation, F273.11)
  — README doc note instead.

All M2-red tests green. Commit.

## M2b — #273 Layer 2 index side: `[Breadcrumb]` property marker + one-hop companion (PRD Design Layer 2, R273.3/4/6/9/12)

**Files:** `libs/spark/MintPlayer.Spark.Abstractions/BreadcrumbAttribute.cs`,
`SparkModelSymbols.cs`, `GenerateIndexGenerator.cs`, `GeneratedIndexInfo.cs`,
`GenerateIndexDiagnostics.cs`, generator + runtime tests.

**M2b-red — generator tests:**

1. Complex property `Address` whose type has `[Breadcrumb] public string City` (stored scalar) →
   companion emitted: index-entity property `AddressSort` with `[IgnoreProperty]`, map line
   `AddressSort = item.Address.City,`, **no** `Index()` declaration for it (undeclared =
   sortable), and **no** SPARK_INDEX_010 for `Address` (the base field still gets
   `FieldIndexing.No`).
2. Marker on a **computed get-only** property carrying `[IgnoreProperty]`
   (`[Breadcrumb, IgnoreProperty] string Crumb => ...`) → same companion
   (`AddressSort = item.Address.Crumb,`) — the sanctioned combination is honored, not diagnosed
   (the one-hop lookup must use a raw member scan like `GetSparkProperties`, NOT
   `IsIndexableProperty`).
3. No marked property on the complex type → no companion + SPARK_INDEX_010 naming the type.
4. Two marked properties on one type → ordinal-min wins + SPARK_INDEX_011.
5. Marker on a `[Reference]` id property / on a collection-typed property → no companion +
   SPARK_INDEX_012, degrade to Layer 1.
6. Entity already declaring a real `{Name}Sort` property → companion skipped + diagnosed (R273.9).
7. Collection of complex whose element type has a marker → still Layer 1 only (no companion).
8. Referenced-assembly variant of tests 1–2 (metadata symbols carry the attribute).
9. **Placement analyzer** (PRD R273.13, test pattern from the existing
   `IgnoredBreadcrumbFieldAnalyzer` tests): marker on a property inside a `[FromIndex]` class →
   Warning; marker on a collection / `[Reference]` id / `TranslatedString` property → Warning;
   marker on a valid scalar/computed property of a plain class → clean (green guard).

**Runtime tests:**

- **Persisted-shape pin** (R273.12, modeled on `TranslatedStringPersistedShapeTests`): a
  `[Breadcrumb, IgnoreProperty]` computed get-only property serializes into the stored document
  JSON at entity AND embedded level; a deliberately null-unsafe getter throws
  `JsonSerializationException` at save (pins the documented failure mode).
- Sort redirect end-to-end (extends `ComplexFieldIndexingTests` / `SortCompanionRedirectTests`):
  generated-shape index with the companion → `QueryExecutor` sort on the complex column name
  redirects to `{Name}Sort` and orders case-insensitively, nulls first, on Corax.

**M2b-fix:**

- `BreadcrumbAttribute`: `AttributeTargets.Property`, parameterless (the class-level template
  form is deleted in M2c; sequence the attribute change with M2c if splitting the commit is
  noisier than combining).
- `SparkModelSymbols`: `GetBreadcrumbProperty(this INamedTypeSymbol)` — one-hop, base chain via
  `GetSparkProperties` raw scan, ordinal-min tiebreak. (No `FindBreadcrumbPath`, no cycle/depth
  machinery — recursion lives in the C# property chain.)
- `Describe()`: for a singular complex property whose type has a marked property `X`, append the
  companion `IndexPropertyInfo` (`Name = {Name}Sort`, `MapExpression = item.{Name}.X`,
  `FieldIndexing = null`, `IsSortCompanion = true`) — rides the TranslatedString-companion
  emission unchanged.
- SPARK_INDEX_011/012 descriptors + release tracking.
- **New standalone placement analyzer** (next free SPARK00x id; no CodeFixProvider — breaks the
  generator suite per `reference_codefix_needs_own_assembly`): the two rules from red test 9.

All green; snapshots updated. Commit.

## M2c — #273 Layer 2 display side: JSON-authoritative templates + embedded-token rendering (PRD R273.10/11)

**Files:** `BreadcrumbAttribute.cs` (delete class form), `SparkModelShape.cs` (delete
attribute-hash lines :64-68), `ModelSynchronizer.cs` (delete attribute-authoritative branch
:705-707; synthesized default = `"{MarkedProperty}"` for marker-carrying types; drift warning),
`Services/Breadcrumb/BreadcrumbResolver.cs` + `EmbeddedBreadcrumbRenderer.cs` (embedded-complex
token arm), `IgnoredBreadcrumbFieldAnalyzer.cs` (SPARK003 **retired** — its subject, the
class-level template, is deleted; the M2b placement analyzer is its successor), demo entities (`HR.Library`, `DemoApp` — delete class-level
attributes; templates already persisted in their model JSON, so files stay put).

**M2c-red — tests:**

1. Resolver: template token naming an embedded complex property renders the embedded type's
   breadcrumb (JSON template, else marked property) against the in-memory value — red today
   (renders `ToString()` type name). Include: null embedded value → empty; template cycle between
   two embedded types → guarded, no hang.
2. `EmbeddedBreadcrumbRenderer` mirror of test 1.
3. Synchronizer: JSON-authored template preserved with the attribute gone (mostly green — pins
   the deletion); synthesized default for a marker-carrying type is `"{MarkedProperty}"` (red);
   drift warning when the marked property is absent from the authored template (red).
4. Model-shape hash: deleting the attribute-hash lines must not move hashes for entities whose
   templates were JSON-persisted (assert on demo fixtures; expected: hash CHANGES because the
   attribute contribution disappears — verify the modelHashes.json regeneration story and record
   it; demos re-sync in M5).

**M2c-fix** per the PRD design. Fixed-point sweep after the synchronizer changes. Commit.

## M3 — #275: provenance-gated `query` preservation (PRD R275.1–R275.3)

**Files:** `libs/spark/MintPlayer.Spark/Services/ModelSynchronizer.cs` (`:556-604`),
`tests/MintPlayer.Spark.Tests/Services/ModelSynchronizerTests.cs`,
`tests/MintPlayer.Spark.Tests/Model/SynchronizeIdempotencyTests.cs`.

**M3a — Red tests** (tamper-on-disk pattern from `MultiLineString_dataType_is_preserved...:216`
and the #274 suite):

1. `Hand_set_query_on_non_reference_attribute_survives_re_synchronize` — sync, JSON-edit a plain
   string attribute to add `"query": "GetPeople"`, sync again → survives (**red**: nulled at
   `:575`). Third run → byte-identical (fixed point).
2. `Removing_Reference_clears_the_stale_derived_query` — seed JSON with `dataType: "Reference"`,
   `referenceType`, `query` for a property that is a plain `string` on the class → after sync,
   `Query == null`, `ReferenceType == null` (green today by accident; pins R275.3 so the fix can't
   regress to pure-preserve).
3. `Reference_attribute_query_is_still_rederived` — tamper a `[Reference]` attribute's query to
   `"GetWrong"` → corrected back (green today; pins R275.2).

**M3b — Fix:** capture `wasReference` before `:563`, replace the ternary at `:575` with the
three-way from the PRD design; console line on the stale-clear (mirroring `:411-415`). Comment
updates at `:569-573` — `query` leaves the "unconditional, including back to null" set; say why.

## M4 — #276: rename retarget + dead-source warning (PRD R276.1–R276.6)

**Files:** `ModelSynchronizer.cs` (`:111-136` + a pre-pass; `CollectQueriesFor` unchanged),
same test files as M3.

**M4a — Red tests:**

1. `Renamed_context_property_retargets_the_existing_query_instead_of_minting_a_duplicate` — two
   context classes over one temp dir (`People` → `Persons`, same entity); tamper `sortColumns`/
   `renderMode` to authored values before the second sync; assert: exactly **one** query, same
   `Id`, `Source == "Database.Persons"`, authored sort columns intact, `Name == "GetPersons"`
   (convention rename). **Red**: two queries, old one dead.
2. `Unpairable_dead_Database_source_is_kept_and_warned` — seed an extra
   `{ "source": "Database.Ghosts" }` query → both queries survive, source unchanged (keep-don't-
   delete pin; assert the console warning via captured writer, `SparkExtensionsPrivateHelpersTests`
   pattern).
3. `Custom_source_queries_pass_through_untouched` — seed `Custom.GetTop`, sync twice →
   byte-identical (green guard).
4. `Ambiguous_multi_rename_falls_back_to_warn_and_mint` — two same-typed properties renamed at
   once → no pairing guess: both old queries kept + warned, both new names minted (red: today
   mints without warning — assert the warning; the query set matches today).
5. `Hand_authored_indexName_is_never_cleared` — seed `indexName: "People/Search"` with no registry
   registration → survives (green guard, R276.5).
6. Fixed-point sweep: each scenario re-synced once more → byte-identical.

**M4b — Fix:** pre-pass before the mint loop, scoped to `Database.*` + matching `EntityType`:
build dead-source and unclaimed-property sets per entity group; exactly-one-each → retarget in
place (source, conventional name+alias rename, console line); otherwise warn per dead source and
fall through to today's minting. Never touch `IndexName`/`UseProjection`/`Custom.*`.

## M5 — Demo adoption, docs, release notes

- **HR `Person` adopts `[GenerateIndex]`** (PRD R273.8): the real-world acceptance case — nested
  `Address` with `[Breadcrumb, IgnoreProperty] string Crumb => $"{Street}, {City}"` (Layer-2
  companion, sortable column), `[Sortable] CarreerJob[]` (Layer 1, stored-not-indexed), scalar
  `Professions` list (stays indexed). Class-level `[Breadcrumb]` attributes on
  `Person`/`Company`/`Profession` deleted (templates already live in the model JSON). Run
  `--spark-synchronize-model` on all four demos to a fixed point; review the HR model diff
  (expected churn — demos are not a compat constraint). Verify the HR app starts and its grid
  renders through the generated index (E2E suite covers Fleet; a targeted `SparkTestDriver` test
  or manual check covers HR).
- Docs: `libs/spark/MintPlayer.Spark/README.md` (synchronize-preservation section: `query`
  preservation + rename retarget + dead-source warning; index docs: coexistence + ordinal-min
  default + `GetRegistrationsForCollectionType`), `docs/guide-search.md` /
  `docs/model_sync_lifecycle_PRD.md` touch-ups where they state the old registry semantics.
- Release notes (PRD acceptance §6), most prominently: **delete the `OnInitialize()`
  complex-field workaround partial** (duplicate `Index()` throws at startup); SPARK_INDEX_004
  removed; SPARK_INDEX_010 added; `IIndexRegistry` breaking change.

## M6 — Batched verification, version bump, PR

- Full test sweep, once: `MintPlayer.Spark.Tests` + `MintPlayer.Spark.SourceGenerators.Tests` +
  idempotency + E2E. Flaky-under-load caveat: re-run named failures in isolation before calling a
  regression.
- Lockstep version bump: all packable libs `10.0.0-preview.54` → `10.0.0-preview.55` (verify the
  current number on `master` first).
- Update PRD/plan status; commit; open the PR (squash), body linking all four issues with
  `Closes #272/#273/#275/#276`.
