# Plan — Issues #286–#290, #292, #293

**PRD:** [issue_286_287_288_289_290_PRD.md](issue_286_287_288_289_290_PRD.md) ·
**Ships in:** `10.0.0-preview.58` · **Branch:** `feat/issue-286-local-credential-opt-out` ·
**PR:** [#291](https://github.com/MintPlayer/MintPlayer.Spark/pull/291) — one combined PR for all seven

**Part 1 (#286–#290) is complete and green on CI.** Part 2 (#292, #293) was added on 2026-08-21.

One commit per milestone; each milestone builds clean; the full suite runs once per part (M6 for Part 1,
M9 for Part 2), with intermediate milestones verified by build + reading, per working convention.

| | Milestone | Issue | State |
|---|---|---|---|
| S1 | Spike: can `MapIdentityApi` output be selectively suppressed? | #286 | ✅ **PASSED** on candidate (a) — `c35d9e3` |
| S2 | Spike: does GitHub sign-in survive removal of the family? | #286 | folded into M1 (see note) |
| M1 | `SparkAuthenticationOptions` + gated server endpoints | #286 | ✅ `e1d8c56` |
| M2 | `GET /spark/auth/capabilities` | #288 | ✅ `e0fc781` |
| M3 | Client route gating + redirect targets that cannot dead-end | #287 | ✅ `34bd43c` |
| M4 | `SparkSignInComponent` — the external-only landing experience | #290 | ✅ `95200a1` |
| M5 | `/connect/*` honours the same mode | #289 | ✅ `64d6e7c` |
| M6 | Demos, guide, release notes, version bumps, full sweep | all | ✅ `2a8de8f` |
| S3 | Spike: can a context property be re-rooted onto an index query? | #293 | **gates M8** |
| M7 | `SynchronizeModels` takes a `Type` | #292 | not started |
| M8 | Context property survives an index binding | #293 | not started |
| M9 | Docs, release notes, sweep for Part 2 | #292 #293 | not started |

**Issues filed for Part 1** (all landed on the numbers the docs already assumed):

| # | Title |
|---|---|
| #286 | Local-credential endpoints are mapped unconditionally and cannot be opted out of |
| #287 | `sparkAuthRoutes()` always emits the local-credential pages, and three redirect targets hardcode `/login` |
| #288 | No server→client channel for auth capabilities, so the two tiers can silently disagree |
| #289 | `MintPlayer.Spark.IdentityProvider` serves a second, ungated local-credential surface at `/connect/*` |
| #290 | No library-provided sign-in landing for external-only applications |

All five were filed and landed on these exact numbers. Part 2 adds
[#292](https://github.com/MintPlayer/MintPlayer.Spark/issues/292) (reported by the owner) and
[#293](https://github.com/MintPlayer/MintPlayer.Spark/issues/293) (found while investigating #292).

---

## Spikes

### S1 — can the endpoints `MapIdentityApi<TUser>()` produces be selectively suppressed?

**Why it is load-bearing:** the entire premise of R3/R4 and of M1. `MapIdentityApi` is Microsoft's, lives
in `Microsoft.AspNetCore.Identity.dll`, is all-or-nothing, and exposes no removal API —
`IEndpointConventionBuilder` can *decorate* endpoints but cannot *unmap* them. If none of the routes below
works, M1 becomes "hand-write the retained endpoints", which is a materially larger and longer-lived
piece of work (it must track upstream Identity changes), so this must be answered first.

**Method:** build a throwaway minimal host and try each candidate in order, asserting against
`EndpointDataSource.Endpoints` (**not** against HTTP status codes — a 404 proves reachability, and the
requirement in R3 is *absence*, which only the data source can demonstrate):

- **(a) Nested route builder + filtered re-publication.** Call `MapIdentityApi<TUser>()` into a throwaway
  `IEndpointRouteBuilder`, read its `DataSources`, and publish only the wanted endpoints into the real
  builder via a custom `EndpointDataSource`. Expected to work and is the preferred outcome — it keeps
  Microsoft's handlers, so no upstream behaviour is re-implemented.
- **(b) A custom `MatcherPolicy` / `EndpointDataSource` decorator** that filters the composite source.
  Fallback if (a)'s route-group conventions (the antiforgery metadata stamped at
  `SparkAuthenticationExtensions.cs:84-101`) do not survive re-publication.
- **(c) Reflection over the internal `IdentityEndpointsExtensions` route table.** Explicitly to be
  *rejected* if it works — recorded here so the next reader knows it was considered and why it was not
  taken: it binds Spark to a private shape that a servicing release can change without notice.

**Trap to avoid:** running the spike inside the existing test host, where Spark's own conventions and the
generated `MapSparkAuthEndpoints` are already present, would leave it ambiguous which registration
produced a surviving endpoint. Run it as a standalone `WebApplication` with *only* `AddIdentityApiEndpoints`
+ the candidate mapper.

**Pass criterion:** for `Disabled`, `EndpointDataSource.Endpoints` contains no route pattern in the R3
set, while `/manage/2fa` and `GET /manage/info` remain, and the antiforgery metadata on the retained
endpoints is intact.

**RESULT — PASSED on candidate (a), first try.** Three probes in
`tests/MintPlayer.Spark.Tests/Authorization/Extensions/LocalCredentialFilterSpike.cs`, all green.
Candidates (b) and (c) were never needed and are not implemented.

The unfiltered route table, read from `EndpointDataSource` (probe 1) — ten endpoints, exactly what F1
claimed, with `/manage/info` appearing twice because GET and POST are separate endpoints:

```
/spark/auth/confirmEmail
/spark/auth/forgotPassword
/spark/auth/login
/spark/auth/manage/2fa
/spark/auth/manage/info
/spark/auth/manage/info
/spark/auth/refresh
/spark/auth/register
/spark/auth/resendConfirmationEmail
/spark/auth/resetPassword
```

After mapping into a throwaway `IEndpointRouteBuilder` and re-publishing the wanted subset through a
fixed `EndpointDataSource` (probe 2):

```
/spark/auth/manage/2fa
/spark/auth/manage/info
/spark/auth/manage/info
```

Probe 3 settles the question that mattered most for safety: **conventions added before materialization
survive re-publication.** The antiforgery stamping at `SparkAuthenticationExtensions.cs:84-101` is still
present as `IAntiforgeryMetadata` on the retained endpoints after the round trip, so candidate (a) does
not silently drop a CSRF control.

Two facts worth carrying into M1: `/manage/info` must be filtered **per HTTP method**, not per route
pattern (R5 keeps GET and drops POST, and they share a pattern); and `MapIdentityApi` generates
confirmation links by route name, so a mode must never keep a link-*generating* endpoint while dropping
its link *target* — `Full`, `SignInOnly` and `Disabled` all satisfy this, but a future fourth mode
might not.

**Negative result →** M1 changes shape: do not call `MapIdentityApi` at all in non-`Full` modes; hand-write
`login`, `refresh`, `manage/2fa`, `GET /manage/info` against `SignInManager`/`UserManager`, and record in
the PRD that `Full` mode keeps Microsoft's implementation while the other two use Spark's. Add a test that
pins response-shape parity between the two implementations so they cannot drift.

**Disposition (revised in M1):** the spike probes were **deleted** rather than kept. Keeping them would
have meant maintaining a second copy of the filtering mechanism in test code, which drifts from the real
one and then pins the copy instead of the product. Everything they proved is now asserted against the
shipped implementation in `LocalCredentialModeTests`: probes 1 and 2 by
`Full_mode_matches_the_pre_change_endpoint_set` + `Disabled_mode_does_not_map_any_local_credential_endpoint`,
and probe 3 — the convention-survival question — by `Antiforgery_metadata_survives_the_filtered_republication`.

### S2 — does GitHub sign-in still work end to end with the local-credential family removed?

**Why it is load-bearing:** F3 establishes by inspection that the external callback provisions inline via
`UserManager`/`SignInManager` and never calls `/register` or `/confirmEmail`. That is an argument, not an
observation, and it is the one regression that would matter most — a framework flag that silently breaks
the only remaining way to sign in would be worse than the problem it fixes.

**Method:** in `Disabled` mode, drive the full flow against a stub OAuth provider (an in-process scheme
standing in for GitHub, emitting an attested `email_verified` claim): `GET /spark/auth/external-login`
→ challenge → stub callback → `/spark/auth/external-login-callback` → assert a new `SparkUser` exists
with `EmailConfirmed = true`, no password hash, an `AddLoginAsync` row, and a valid `ApplicationScheme`
cookie. Then `GET /spark/auth/me` must report `isAuthenticated: true`, and `POST /spark/auth/csrf-refresh`
must rotate the XSRF cookie.

**Pass criterion:** the whole chain succeeds with zero endpoints from the R3 set present.

**Negative result →** the coupling F3 ruled out is real after all; identify it and either retain the
specific endpoint (amending R3, with the reason recorded) or replicate the step inline in the callback.

**Disposition:** landed as a permanent test in `tests/MintPlayer.Spark.Tests/Authorization/Extensions/`
(`External_login_provisions_a_user_when_local_credentials_are_disabled`) — AC 3.

**Sequencing note (deviation from the original plan):** S2 was written to run before M1, but it needs the
`LocalCredentials` mode to exist in order to configure `Disabled`, and its disposition was always "lands
as a test" rather than "throwaway probe". It is therefore executed **inside M1** rather than ahead of it.
This does not weaken the gate: S1 alone determines M1's *shape*, and S1 passed. S2 determines whether the
result is *correct*, and M1 does not ship until it is green.

### Not spiked: the client-side route omission

`sparkAuthRoutes` builds a plain array locally with five unconditional `import()` calls in one function
(`routes/src/spark-auth-routes.ts:27-62`), the package declares `"sideEffects": false`, and each page is
its own secondary entry point. Skipping a branch of `import()` calls is ordinary esbuild dead-code
elimination — there is no unknown here. The only subtlety (the call site must be removed, not merely the
route object, or the lazy chunks are still emitted) is a *build-output assertion in M3*, not a question
whose answer changes the design.

---

## M1 — `SparkAuthenticationOptions` + gated server endpoints (#286) — R1–R8, R15, R16; AC 1, 2, 4, 5, 6

**Files:**
- `libs/authorization/MintPlayer.Spark.Authorization/Configuration/SparkAuthenticationOptions.cs` (new)
- `libs/authorization/MintPlayer.Spark.Authorization/Configuration/SparkLocalCredentials.cs` (new — enum `Full | SignInOnly | Disabled`)
- `libs/authorization/MintPlayer.Spark.Authorization/Extensions/SparkAuthenticationExtensions.cs` (split `MapSparkIdentityApi`, `:67-219`)
- `libs/spark/MintPlayer.Spark/Extensions/SparkBuilderExtensions.cs` (`:47-70` — new options parameter, registry wiring)
- `libs/authorization/MintPlayer.Spark.Authorization/Configuration/ExternalLoginOptions.cs` (**delete** — R16)
- `libs/all_features/MintPlayer.Spark.AllFeatures/SparkFullOptions.cs` (surface the mode on the bundle)

- `AddAuthentication<TUser>(Action<SparkAuthenticationOptions>? configure = null, Action<IdentityOptions>? configureIdentity = null, Action<IdentityBuilder>? configureProviders = null)`. The new parameter goes **first** and is optional, so existing positional callers keep compiling; the demos all use named arguments already.
- Split `MapSparkIdentityApi<TUser>` per R8: extract `MapLocalCredentialApi<TUser>(mode)` holding the gated `MapIdentityApi` call currently at `:83`, leaving `MapSparkAuthEndpoints()` (`:104`), external-login (`:107`) and the callback (`:134`) unconditional.
- Keep `AddIdentityApiEndpoints<TUser>()` at `:45` in every mode (R7) — it is the service registration the external callback depends on. Add a comment saying so; this is the single most likely thing for a future reader to "clean up".
- Fix the misattributing doc comment at `:62-65` (F11) while in the file.
- Antiforgery stamping (`:84-101`) must be applied to whatever survives the filter, not to the pre-filter set.
- R15 throw: validate at `MapSpark()` time (where `IAuthenticationSchemeProvider` is resolvable), not at `AddAuthentication` time (where providers registered later by `configureProviders` are not yet visible). Message must name the fix, in the style of `SparkBuilderRateLimiterExtensions`' existing throws.
- `SparkLocalCredentials.Full` must remain the default on every path, including the `SparkFullOptions` bundle.

**Tests** (`tests/MintPlayer.Spark.Tests/Authorization/Extensions/MapSparkIdentityApiTests.cs` — extends the existing file):

| Test | Pins |
|---|---|
| `Full_mode_matches_the_pre_change_endpoint_set` | AC 5 — a golden route-pattern list captured from `master` before the change; red if any mode leaks into the default |
| `Disabled_mode_does_not_map_any_local_credential_endpoint` | R3, AC 1 — asserts over `EndpointDataSource`, not status codes |
| `Disabled_mode_keeps_the_universal_auth_endpoints` | R6, AC 2 |
| `SignInOnly_removes_only_self_service_registration` | R4, AC 4 |
| `Disabled_mode_removes_post_manage_info_but_keeps_get` | R5 |
| `Disabled_without_an_external_provider_throws_on_startup` | R15, AC 6 |
| `Identity_services_are_registered_in_every_mode` | R7 — resolves `UserManager<TUser>`/`SignInManager<TUser>` from the container under `Disabled` |

## M2 — `GET /spark/auth/capabilities` (#288) — R9; AC 7

**Files:**
- `libs/authorization/MintPlayer.Spark.Authorization/Endpoints/GetCapabilities.cs` (new — follows `Endpoints/GetCurrentUser.cs:8`, picked up by the endpoint source generator via `SparkAuthGroup.Prefix`)
- `libs/authorization/MintPlayer.Spark.Authorization/Extensions/SparkAuthenticationExtensions.cs` (make the mode available to the handler)

- Anonymous, like `/me`. Shape: `{ localCredentials: "Full"|"SignInOnly"|"Disabled", externalProviders: [{ scheme, displayName }] }`.
- Providers come from `IAuthenticationSchemeProvider.GetAllSchemesAsync()`, filtered to schemes with a `DisplayName` (which is what distinguishes an interactive external provider from `ApplicationScheme`/`BearerScheme`/module-certificate schemes). Shared with M1's startup guard as `ExternalAuthenticationSchemes`, so "can a human sign in with this?" has one answer.
- **Design change made during M1/M2:** the reported mode is **derived from the route table**, not read back from `SparkAuthenticationOptions`. Reporting the configured value would let the endpoint claim a surface that was never mapped, or deny one that was — exactly the drift this endpoint exists to prevent. Deriving it from the endpoints that exist makes the answer true by construction.
- No caching header initially; it is one small allocation per app boot on the client.

**Tests** (`tests/MintPlayer.Spark.Tests/Authorization/Extensions/`):

| Test | Pins |
|---|---|
| `Capabilities_reports_the_configured_mode_and_providers` | R9, AC 7 |
| `Capabilities_is_reachable_anonymously` | R9 |
| `Capabilities_omits_non_interactive_schemes` | R9 — bearer/cookie/certificate schemes excluded |

## M3 — Client route gating + redirect targets (#287) — R10, R11, R12; AC 8, 9

**Files:**
- `libs/node_packages/ng-spark-auth/models/src/auth-route-config.ts` (add `localCredentials?: 'full' | 'sign-in-only' | 'disabled'` to `SparkAuthRouteConfig`)
- `libs/node_packages/ng-spark-auth/routes/src/spark-auth-routes.ts:27-62` (branch the children array)
- `libs/node_packages/ng-spark-auth/models/src/auth-config.ts` (document `loginUrl` as the authoritative redirect target)
- `libs/node_packages/ng-spark-auth/guards/src/spark-auth.guard.ts:15`, `interceptors/src/spark-auth.interceptor.ts:26`, `auth-bar/src/spark-auth-bar.component.html:5`

- The three redirect consumers keep using `config.loginUrl` — R12 is satisfied by making it *authoritative*, not by adding a fourth concept. An external-only app sets `loginUrl: '/sign-in'`. **Landed as a shared `resolveSignInUrl(config, router)` in `models/src/sign-in-redirect.ts`** rather than three copies of a property read: it warns once in dev mode when `loginUrl` matches no registered route, which is F6's silent failure. One helper also means the auth bar cannot link somewhere the guard would not send you.
- `SparkAuthRouteConfig` was split into `SparkAuthRouteEntries` (the five routable pages) plus the new `localCredentials` option, because `SparkAuthRoutePaths` is `Required<Record<keyof SparkAuthRouteConfig, string>>` — adding the option directly to that interface would have demanded a `localCredentials` *path*.
- `SparkAuthRoutePaths` stays `Required<...>` (R11) — the family drops as a unit, so no template dereferences a missing key. Do **not** relax the type; it is what makes the star-graph in F6 safe.
- The `import()` calls for the omitted components must sit inside the untaken branch (R10), not merely be filtered out of the returned array afterwards — otherwise esbuild still emits the chunks. This is exactly what AC 8's build assertion checks.
- `two-factor` drops with the family in `disabled` (F5: reachable only from `SparkLoginComponent`), and is retained in `sign-in-only`.

**Tests** (`libs/node_packages/ng-spark-auth/**/*.spec.ts`):

| Test | Pins |
|---|---|
| `sparkAuthRoutes omits the local-credential children when disabled` | R10, AC 8 |
| `sparkAuthRoutes emits today's five children by default` | R10 — red if the default shifts |
| `sign-in-only keeps login, forgot-password, reset-password, two-factor` | R4 parity on the client |
| `the guard navigates to config.loginUrl` / `the interceptor navigates to config.loginUrl on 401` | R12, AC 9 |
| `a loginUrl matching no route warns in dev mode` | R12 — pins the silent-failure fix |

## M4 — `SparkSignInComponent` (#290) — R13; AC 9, 11

**Files:**
- `libs/node_packages/ng-spark-auth/sign-in/{ng-package.json,index.ts,src/spark-sign-in.component.{ts,html}}` (new secondary entry point — additive, R11)
- `libs/node_packages/ng-spark-auth/core/src/spark-auth.service.ts` (add `capabilities()` reading M2's endpoint)
- `libs/node_packages/ng-spark-auth/routes/src/spark-auth-routes.ts` (emit a `sign-in` route when the mode is not `full` — emitting it by default would change the routes of every app that never opted in)
- `libs/authorization/MintPlayer.Spark.Authorization/App_Data/translations.json` (three keys, en/fr/nl)

- Renders one button per provider from `/spark/auth/capabilities`, calling the existing
  `loginWithProvider(scheme, options)` (`core/src/spark-auth.service.ts:84-131`) — **no hardcoded scheme
  string**, which is the specific defect F7 found in every consumer today.
- Signals + `OnPush`, matching the po-form convention.
- When capabilities reports `localCredentials !== 'disabled'`, the component also links to the login route
  — so it works as a combined landing page, not only as an external-only fallback.
- Mismatch reporting (per the PRD's Design note): if the build-time mode says `disabled` but the server
  reports `Full`, warn in dev mode. This is the drift F8 says is invisible today.

**Tests:**

| Test | Pins |
|---|---|
| `the sign-in page renders one button per reported provider` | R13 |
| `clicking a provider button calls loginWithProvider with its scheme` | R13 |
| `a build-time/server mode mismatch warns in dev mode` | F8 |

## M5 — `/connect/*` honours the same mode (#289) — R14; AC 10

**Files:**
- `libs/identity_provider/MintPlayer.Spark.IdentityProvider/Configuration/SparkIdentityProviderOptions.cs`
- `libs/identity_provider/MintPlayer.Spark.IdentityProvider/Extensions/SparkIdentityProviderExtensions.cs:112-113`

- **Resolved:** the IdP package already `ProjectReference`s `MintPlayer.Spark.Authorization`, so no duplicate
  flag was needed. `AddAuthentication` now publishes `SparkAuthenticationOptions` as a singleton and the IdP
  resolves it, falling back to `Full` when the provider is used without `AddAuthentication` — nothing has
  expressed an opinion in that case, so the provider keeps its own login page.
- In `Disabled`, do not map `GET|POST /connect/login` or `/connect/two-factor`. The OIDC protocol endpoints
  (`/connect/token`, `/connect/authorize`, `/.well-known/*`) are unaffected — an IdP federating to an
  upstream provider still needs all of them.
- `Endpoints/Login.cs:81-92` resolves `SignInManager<>` reflectively; confirm the reflective resolution is
  not invoked at mapping time, or the gated path will still touch it.

**Tests** (`tests/MintPlayer.Spark.Tests/IdentityProvider/`, via `OidcTestHost`):

| Test | Pins |
|---|---|
| `Disabled_mode_removes_the_identity_provider_login_pages` | R14, AC 10 |
| `Disabled_mode_keeps_the_oidc_protocol_endpoints` | R14 — the discriminating half |

## M6 — Demos, guide, release notes, version bumps, sweep — R17; AC 11

- **Convert WebhooksDemo to external-only** (`Demo/WebhooksDemo/WebhooksDemo/Program.cs:29-38`,
  `ClientApp/src/app/app.routes.ts:11`, `app.config.ts`). F5 established it is *already* effectively
  external-only and ships five unused pages, so it is the honest demo of the feature — and AC 11's browser
  run. Leave HR/Fleet on `Full` so both modes stay exercised.
- Re-sync all four demo apps; expect only the WebhooksDemo auth changes and nothing else (minimal-diff
  check, per the standing preference).
- `docs/guide-authentication-schemes.md` — new section: the three modes, the exact endpoint set each maps,
  the abuse surface each closes (lift F2's table), the external-only recipe end to end (server flag +
  `sparkAuthRoutes` flag + `loginUrl` + `SparkSignInComponent`), and the D3 note that the Bearer scheme
  stays registered without an issuer in `Disabled`. Update the README guide-table row (`README.md:148-169`).
- `docs/release-notes-preview-58.md` — lead with the `AddAuthentication<TUser>` signature change and the
  `ExternalLoginOptions` deletion (R16), then the feature.
- Version bumps: all 21 `MintPlayer.Spark.*` `<Version>` → `10.0.0-preview.58`;
  `@mintplayer/ng-spark-auth` → `22.2.0` (new entry point + new config fields — additive, hence minor).
  `@mintplayer/ng-spark` is **not** bumped — its TS surface is untouched. Deliberately **not** bumped:
  the `^22.0.0` ranges in the HR and Fleet `ClientApp/package.json` files, which already admit `22.2.0`
  (and resolve to the workspace source regardless).
- Full test suite + E2E; fix fallout. Re-run any failure in isolation before calling it a regression
  (the suite is flaky under load, and the E2E teardown hang from #277 is still open).

---

## Verification

**Part 1 — CI green on PR #291 at `2a8de8f`:** `MintPlayer.Spark.Tests` 1582 ·
`SourceGenerators.Tests` 197 · `E2E.Tests` 78 · `Client.Tests` 38 — **1895 passed, 0 failed**, plus
GitGuardian. (A local sweep was started but killed before it reported; CI is the evidence.)

The checks below are aimed at whether the work could be *fake* rather than at coverage:

- **AC 1 must discriminate.** `Disabled_mode_does_not_map_any_local_credential_endpoint` asserts over
  `EndpointDataSource`. Re-run it against a shadowing-middleware implementation (D2's rejected design) —
  it must **fail**, because the endpoints would still be in the table. A status-code-only assertion would
  pass and prove nothing.
- **AC 5 must discriminate.** `Full_mode_matches_the_pre_change_endpoint_set` uses a golden list captured
  from `master`. Re-run with `Disabled`'s filter accidentally applied to `Full` — it must fail.
- **AC 3 must discriminate.** Re-run S2's landed test with `AddIdentityApiEndpoints` removed (F4's trap) —
  it must fail on DI resolution, proving the test exercises real provisioning rather than a stub.
- **AC 8 must discriminate.** The build assertion must inspect emitted chunk names, not the route array.
  Re-run with the route object filtered *after* the `import()` calls — it must fail.
- **What will not be run locally:** the GitHub OAuth round trip against real github.com (S2 uses a stub
  scheme). Watch CI and the manual browser run of WebhooksDemo for the real handshake; the stub cannot
  catch a provider-side redirect-URI or scope regression.

---

# Part 2 — Issues #292 and #293

Added to this branch on 2026-08-21, after M1–M6 had landed and CI was green. Unrelated to the auth
work; carried in the same PR because the consumer needs both in one release.

## S3 — can a context property's composed query be re-rooted onto an index-backed query?

**Why it is load-bearing:** it decides whether M8 delivers the feature or merely stops the bleeding.
R293.1 accepts either outcome, but they are different pieces of work and the choice must be evidence-led.

**The question, concretely.** The property getter returns
`Session.Query<Account>().Where(a => a.UserId == x)` — an `IRavenQueryable<Account>` whose `Expression`
is a `MethodCallExpression` over a root query. The index path wants
`Session.Query<Account, Accounts_Index>()`. Can the property's expression be replayed onto the index
query — visit the property's expression, replace the innermost root with the index query's expression,
and hand the result to the index query's `IQueryProvider.CreateQuery` — such that the generated RQL
contains **both** the index and the predicate?

**Method:** a standalone test against a real embedded RavenDB (`SparkTestDriver`), with a declared
index over a seeded collection. Build both queryables, re-root one onto the other with an
`ExpressionVisitor`, then assert on **the generated RQL string** (`ToString()` on the document query,
or `RavenQueryInspector.ToString()`) — not merely on the returned rows. Rows could come back correct by
accident on a small fixture; the RQL is what proves the index and the predicate are both present.

Then execute it and assert the filtered row set, so the spike covers both "it compiles into the right
query" and "the query runs".

**Trap to avoid:** asserting only that no exception is thrown. RavenDB's LINQ provider is tolerant, and
a silently-dropped `Where` is exactly the failure mode under investigation — the spike must be able to
tell "predicate applied" from "predicate ignored", which only the RQL does.

**Pass criterion:** the RQL names the index **and** contains the predicate, and execution returns only
the matching rows.

**Negative result →** M8 becomes fail-closed instead: detect that the property's expression is
non-trivial (its `Expression` is not the bare root) while an index binding applies, and throw an
`InvalidOperationException` naming the property, the index and the fix. Strictly better than today's
silent discard, and it keeps the door open for re-rooting later.

**Disposition:** whichever branch wins, the spike lands as a permanent test — either pinning the
re-rooted RQL, or pinning the diagnostic.

### Not spiked: the `Type` refactor (#292)

Nothing to learn. `ModelSynchronizer.cs` references its `SparkContext` parameter on exactly one line
(`:39`, `GetType()`), every downstream API is already `Type`-based, and the change is a signature swap.
The risk in #292 is not feasibility, it is the base-type footgun — and that is closed by a guard (M7),
not by an experiment.

---

## M7 — `SynchronizeModels` takes a `Type` (#292) — R292.1–R292.5

**Files:**
- `libs/spark/MintPlayer.Spark/Services/ModelSynchronizer.cs` (`:16` interface member, `:37-39` method)
- `libs/spark/MintPlayer.Spark/Extensions/SparkDevelopmentExtensions.cs` (`:82` constraint, `:92`/`:94`
  `new TContext()`, `:170` `Verify`, `:208` `Synchronize`, `:287-323` `TryCreateRegisteredContext`)

- `SynchronizeModels(SparkContext)` → `SynchronizeModels(Type contextType)`; delete the `GetType()` line.
- `TryCreateRegisteredContext` becomes `TryResolveRegisteredContextType(IServiceCollection, out Type)`:
  keep the "no registration" and "null `ImplementationType`" branches verbatim (R292.5), delete the
  ctor check and `Activator.CreateInstance` (R292.2), and add the assignability/abstract guard (R292.3).
- Drop `new()` at `:82`; pass `typeof(TContext)`.
- **R292.4 guard** in `ModelSynchronizer` immediately before `WriteModelHashes` (`:279`): if the
  discovered root set is empty **and** `App_Data/Model` already contains entity JSON, do not write the
  hash file — write to `Console.Error` and signal misconfiguration. Rationale in the code comment: the
  damage is a hash file certifying an empty model over a populated directory, and it stays invisible
  until Production startup.

**Tests** (`tests/MintPlayer.Spark.Tests/Builder/SparkExtensionsTests.cs` + `Services/ModelSynchronizerTests.cs`):

| Test | Pins |
|---|---|
| `A_context_with_constructor_dependencies_synchronizes` | R292.2 — **red on master**, the whole point of #292 |
| `A_context_with_constructor_dependencies_verifies` | R292.2 for the CI gate |
| `Synchronizing_the_base_SparkContext_type_is_rejected` | R292.3 — exit 2, no files written |
| `Synchronizing_an_abstract_context_type_is_rejected` | R292.3 |
| `A_context_with_no_query_roots_does_not_overwrite_existing_model_hashes` | R292.4 — the destructive case; assert the pre-existing `modelHashes.json` is byte-identical afterwards |
| `A_factory_registered_context_still_reports_misconfigured` | R292.5 — unchanged behaviour |

The 86 existing `SynchronizeModels(new SomeContext())` call sites become `typeof(SomeContext)`. Mechanical,
but re-read the diff: a call site that silently became `typeof(SparkContext)` is exactly the bug R292.3
guards against, and it must not appear in our own tests.

## M8 — a context property survives an index binding (#293) — R293.1, R293.2

**Files:**
- `libs/spark/MintPlayer.Spark/Services/QueryExecutor.cs` (`:145-169`)
- `docs/guide-queries-and-sorting.md` (R293.2)

Shape depends on S3:

- **S3 passes →** re-root the property's expression onto the index query instead of discarding it.
  Keep the fresh index query as the base when the property's expression *is* the bare root, so the
  common case emits exactly today's RQL and nothing regresses.
- **S3 fails →** detect a non-trivial property expression under an index binding and throw, naming the
  property, the index and the remedy.

Either way, the bare-root case (`=> Session.Query<Account>()`) must be byte-identical to today — that is
every existing context property in the repo and in the demos, and a regression there would be broad.

**Tests** (`tests/MintPlayer.Spark.Tests/Endpoints/Queries/` + `Services/`):

| Test | Pins |
|---|---|
| `A_filtered_context_property_keeps_its_predicate_under_an_index_binding` | R293.1 — **red on master**, currently returns every row |
| `A_bare_context_property_under_an_index_binding_is_unchanged` | the no-regression floor |
| `A_filtered_context_property_without_an_index_binding_still_filters` | pins that the non-index path was never broken |

## M9 — docs, release notes, sweep (rides with M7/M8)

- Extend `docs/release-notes-preview-58.md` with #292 (the lifted constraint, and the new
  rejection cases) and #293.
- `docs/guide-queries-and-sorting.md`: a context property may now depend on injected services; it scopes
  the grid only and is **not** an authorization boundary (R293.2).
- Full suite + E2E. CI on PR #291 is the gate; it was green at `2a8de8f`
  (1582 + 197 + 78 + 38 = **1895 passed, 0 failed**), so any new failure is attributable to M7/M8.
