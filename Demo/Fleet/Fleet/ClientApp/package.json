{
  "name": "@spark-demo/fleet-demo",
  "version": "0.0.0",
  "scripts": {
    "start": "nx run @spark-demo/fleet-demo:serve",
    "build": "nx run @spark-demo/fleet-demo:build",
    "test": "nx run @spark-demo/fleet-demo:test"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "dependencies": {
    "@mintplayer/ng-spark-auth": "^0.0.5"
  }
}
