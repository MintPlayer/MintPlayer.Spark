{
  "name": "@spark-demo/webhooks-demo",
  "version": "0.0.0",
  "scripts": {
    "start": "nx run @spark-demo/webhooks-demo:serve",
    "build": "nx run @spark-demo/webhooks-demo:build",
    "test": "nx run @spark-demo/webhooks-demo:test"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true
}
