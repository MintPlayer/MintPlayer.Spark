{
  "name": "@spark-demo/hr-demo",
  "version": "0.0.0",
  "scripts": {
    "start": "nx run @spark-demo/hr-demo:serve",
    "build": "nx run @spark-demo/hr-demo:build",
    "test": "nx run @spark-demo/hr-demo:test"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "dependencies": {
    "@mintplayer/ng-spark-auth": "^0.0.5"
  }
}
